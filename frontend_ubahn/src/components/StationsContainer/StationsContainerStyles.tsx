import styled from "styled-components";

export const StationsContainerWrapper = styled.div`

   margin: 1em;
   padding: 1em;
   display: flex;
   flex-direction: row;
   flex-wrap: wrap;
   justify-content: left;
`